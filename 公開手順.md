# 株式会社大東ホームページ 公開手順

5ページ構成のホームページです。

- `index.html`：トップページ
- `business.html`：事業内容ページ
- `works.html`：施工実績ページ
- `recruit.html`：採用情報ページ
- `company.html`：会社概要・お問い合わせページ
- `site-data.js`：会社情報・採用情報・施工実績の編集用データ
- `かんたん編集.html`：入力フォームで `site-data.js` を作るための編集ページ

## 1. 編集する場所

基本的には `かんたん編集.html` を使うと、HTMLを直接触らずに更新できます。

- 会社情報を変える：`かんたん編集.html` を開いて、会社情報を入力します。
- 採用条件を変える：`かんたん編集.html` を開いて、採用情報を入力します。
- 施工実績を増やす：`かんたん編集.html` を開いて、施工実績を追加します。
- 画像を変える：画像を `images` フォルダに入れて、`かんたん編集.html` の画像欄で選びます。
- 入力後：`データを更新` を押し、保存画面で今の `site-data.js` を選んで上書きします。
- 上書き保存が使えないブラウザでは、`site-data.js` がダウンロードされます。その場合は同名ファイルと置き換えます。
- 色を変える：各ページ上部の `<style>` 内、`:root` の色コードを編集します。
- 大きなレイアウトを変える：各ページのHTMLを編集します。

## 2. 画像の入れ方

写真は `images` フォルダに入れてください。

よく使う画像名は下記です。

- `images/hero.jpg`：トップページの大きな背景写真
- `images/top-business.jpg`：トップページの事業内容カード
- `images/top-works.jpg`：トップページの施工実績カード
- `images/top-recruit.jpg`：トップページの採用情報カード
- `images/top-company.jpg`：トップページの会社概要カード
- `images/strength.jpg`：トップページの強み写真
- `images/business-hero.jpg`：事業内容ページのメイン写真
- `images/business-tech.jpg`：事業内容ページの技術説明写真
- `images/works-hero.jpg`：施工実績ページのメイン写真
- `images/works-01.jpg`：施工実績ページの実績1写真
- `images/works-02.jpg`：施工実績ページの実績2写真
- `images/works-03.jpg`：施工実績ページの実績3写真
- `images/recruit-hero.jpg`：採用情報ページのメイン写真
- `images/recruit-message.jpg`：採用情報ページのスタッフ写真
- `images/company-hero.jpg`：会社概要ページのメイン写真
- `images/og-image.jpg`：SNSやチャットでURLを共有した時に表示される画像

別の名前の画像を使う場合は、`かんたん編集.html` の「登録済み画像一覧」に `images/ファイル名.jpg` を1行追加してから選びます。

## 3. GitHubで公開する流れ

1. GitHubで新しいリポジトリを作成します。
   - 例：`daito-recruit-site`
   - 公開用なら `Public` を選びます。

2. このフォルダ内のファイルをアップロードします。
   - 必須：`index.html`、`business.html`、`works.html`、`recruit.html`、`company.html`、`site-data.js`、`site-render.js`
   - 編集用：`かんたん編集.html`
   - 任意：`images` フォルダ内の画像ファイル、`公開手順.md`

3. GitHub Pages を有効にします。
   - リポジトリ画面の `Settings` を開きます。
   - 左メニューの `Pages` を開きます。
   - `Build and deployment` の `Source` を `Deploy from a branch` にします。
   - `Branch` を `main`、フォルダを `/root` にして保存します。

4. 数分待つと、GitHub Pages のURLが表示されます。
   - 例：`https://ユーザー名.github.io/daito-recruit-site/`

## 4. お名前ドットコムのドメインをつなぐ流れ

GitHub Pages 側で独自ドメインを設定してから、お名前ドットコム側のDNSを設定します。

### `www`付きドメインで公開する場合

例：`www.example.com`

- GitHub Pages の `Custom domain`：`www.example.com`
- お名前ドットコムのDNS：
  - ホスト名：`www`
  - TYPE：`CNAME`
  - VALUE：`ユーザー名.github.io`

### ルートドメインで公開する場合

例：`example.com`

- GitHub Pages の `Custom domain`：`example.com`
- お名前ドットコムのDNSに、下記4つの `A` レコードを設定します。
  - `185.199.108.153`
  - `185.199.109.153`
  - `185.199.110.153`
  - `185.199.111.153`

## 5. 公開後の確認

- スマートフォンで表示確認します。
- メニューから5ページすべて開けるか確認します。
- 電話番号をタップして発信画面になるか確認します。
- `かんたん編集.html` で会社情報や採用情報を確認します。
- 写真が表示されているか確認します。
- URLをLINEやチャットに貼った時の表示用に、`images/og-image.jpg` を用意しておくと見栄えが良くなります。

## 6. 更新するとき

通常の更新は `かんたん編集.html` で行います。

1. `かんたん編集.html` を開きます。
2. 文章・数字・画像を変更します。
3. `データを更新` を押します。
4. 保存画面が出たら、今の `site-data.js` を選んで上書きします。
5. 上書き保存が使えないブラウザでは、ダウンロードされた `site-data.js` を古い `site-data.js` と置き換えます。
6. 更新した `site-data.js` をGitHubにアップロードします。
7. 新しい画像を使う場合は、画像ファイルも `images` フォルダへアップロードします。
8. 数分後にホームページへ反映されます。

## 7. 何をアップロードすればいいか

文章・数字だけを変えた場合：

- `site-data.js`

画像だけを追加・変更した場合：

- `site-data.js`
- `images` フォルダ内の追加・変更した画像

ページの構成やデザインを変えた場合：

- 変更したHTMLファイル
- 必要に応じて `site-data.js`
- 必要に応じて画像ファイル
